# Harness Backlog — engram

`/harness next` pops the topmost bullet from `## Queued`. Bullets are FIFO.
Each bullet should describe a feature in 1–3 sentences, concrete enough for the spec phase to chew on. Place context, constraints, and "why" inline.

## Queued

- 为 engram 加一个 creativity 维度（PHRONOS-style work-cognition 那种思路也行，但范围限定在 engram psychology preset 内）。dimension 包含 backbone prompt + 默认 weight + 测试数据。验证 harness 主链路使用。

## Dispatched

<!-- 取走的条目移动到这里，附带时间戳 -->
